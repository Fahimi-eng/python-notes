# این تمرین به‌صورت هدف‌دار طوری طراحی شده که بخشی از یادگیری شما از طریق جست‌وجو و مطالعه‌ی بیشتر انجام شود.
# بنابراین اگر در قسمت‌هایی مانند فایل setup.py یا نحوه ساخت پکیج سؤال داشتید، پیشنهاد می‌شود با کمی تحقیق و سرچ در منابع مختلف موضوع را کامل‌تر یاد بگیرید.

# یک کتابخانه می‌خواهد سیستمی برای مدیریت کتاب‌های خود داشته باشد. این سیستم شامل قابلیت‌هایی مانند اضافه کردن کتاب، حذف کتاب، جستجوی کتاب، و نمایش همه‌ی کتاب‌ها است. شما باید این سیستم را به‌صورت ماژولار پیاده‌سازی کنید و قابلیت نصب ماژول را از طریق pip و PyPi بررسی کنید.

 
# ۱. استفاده از ماژول‌ها و پکیج‌ها (Modules and Packages)
# وظیفه شما:
# ۱. یک ماژول به نام library.py بسازید که شامل کلاس Library باشد. این کلاس باید ویژگی‌های زیر را داشته باشد:

# یک لیست از کتاب‌ها که کتاب‌ها را ذخیره کند.
# متد add_book(title, author) برای اضافه کردن کتاب.
# متد remove_book(title) برای حذف کتاب.
# متد search_book(title) برای جستجوی کتاب.
# متد show_books() برای نمایش همه‌ی کتاب‌ها.
# ۲. یک فایل جدید به نام main.py بسازید که از library.py استفاده کند و سیستم مدیریت کتابخانه را اجرا کند.

 
# ۲. استفاده از __name__ == "__main__"
# وظیفه شما:
# در main.py بررسی کنید که آیا این فایل مستقیماً اجرا شده است یا نه.
# اگر مستقیماً اجرا شده بود، یک منوی ساده برای اضافه کردن، حذف کردن و جستجوی کتاب‌ها نمایش دهید.
 
# ۳. تبدیل ماژول به یک پکیج نصبی (pip install)
# وظیفه شما:
# ۱. پکیج خود را برای نصب محلی آماده کنید:

# یک پوشه به نام mylibrary بسازید و فایل library.py را داخل آن قرار دهید.
# یک فایل __init__.py داخل mylibrary/ بسازید.
# یک فایل setup.py برای نصب پکیج ایجاد کنید.
# ۲. پکیج را نصب کنید:

# دستور pip install . را اجرا کنید تا پکیج به‌صورت محلی نصب شود.
# سپس آن را در main.py ایمپورت کنید و تست کنید.

 

# راهنمایی 1: آشنایی با فایل setup.py
# فایل setup.py در واقع نقش شناسنامه‌ی پکیج پایتون شما را دارد.
# این فایل به پایتون و ابزار نصب‌کننده‌ی آن (pip) اعلام می‌کند که پکیج شما شامل چه بخش‌هایی است، نام و نسخه‌ی آن چیست، و در زمان نصب چه فایل‌ها یا وابستگی‌هایی باید در نظر گرفته شوند.

# زمانی که دستور زیر را در ترمینال اجرا می‌کنید:

# pip install .
# پایتون به دنبال فایل setup.py می‌گردد تا بفهمد پکیج چگونه باید نصب شود و در کجا قرار گیرد.

# یک نمونه‌ی ساده از فایل setup.py به شکل زیر است:

# from setuptools import setup, find_packages

# setup(
# name=“mylibrary”,               # نام پکیج
# version=“1.0.0”,                # نسخه پکیج
# author=“Your Name”,             # نام نویسنده
# description=“A simple library management system”,  # توضیح کوتاه
# packages=find_packages(),       # جست‌وجو و شناسایی خودکار پوشه‌های پکیج
# )
# ا وجود این فایل، پکیج شما قابل نصب و استفاده به‌صورت محلی خواهد بود.
# برای نمونه، پس از نصب می‌توانید در فایل‌های دیگر از کد خود استفاده کنید:

# from mylibrary.library import Library
# نکته: فایل setup.py برای پایتون مانند یک نقشه‌ راه است؛ بدون آن، سیستم نصب نمی‌داند چه فایل‌هایی متعلق به پکیج شما هستند.

 

# ‌راهنمایی 2: کلیدواژه‌های پیشنهادی برای جست‌وجو
# برای مطالعه و یادگیری بیشتر درباره ساخت پکیج‌ها در پایتون، می‌توانید از کلیدواژه‌های زیر استفاده کنید:

# - ساخت پکیج در پایتون how to create a Python package  

# - آموزش فایل python setup.py explained  

# - معرفی ماژول‌ها و پکیج‌ها در پایتون python modules and packages tutorial

# - نحوه نصب پکیج how pip install works in Python  

# - ساخت پکیج محلی (Local Package) install local python package pip install dot  

# - ساختار پوشه پکیج پایتون python package folder structure

#---------------------------------------------------------------------------------------------------


from mylibrary import Library

lib = Library()
lib.add_book("1984", "George Orwell")